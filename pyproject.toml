[project]
name = "UAP"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "Arya Mandala Putra", email = "aryamp33@gmail.com"},
]
dependencies = ["streamlit>=1.52.2", "tensorflow>=2.20.0", "scikit-learn>=1.7.2", "joblib>=1.5.3", "category-encoders>=2.8.1", "pytorch-tabnet>=4.1.0"]
requires-python = ">=3.10"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
